import React, {FC} from 'react';
import MuiCreateIcon from '@material-ui/icons/Create';

import './CreateIcon.scss';

const CreateIcon: FC = () => {
  return <MuiCreateIcon className="CreateIcon" />;
};

export default CreateIcon;
