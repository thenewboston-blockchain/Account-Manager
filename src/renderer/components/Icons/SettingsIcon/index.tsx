import React, {FC} from 'react';
import MuiSettingsIcon from '@material-ui/icons/Settings';

import './SettingsIcon.scss';

const SettingsIcon: FC = () => {
  return <MuiSettingsIcon className="SettingsIcon" />;
};

export default SettingsIcon;
