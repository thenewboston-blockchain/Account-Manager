import React, {FC} from 'react';
import MuiPlayCircleFilledIcon from '@material-ui/icons/PlayCircleFilled';

import './PlayCircleFilledIcon.scss';

const PlayCircleFilledIcon: FC = () => {
  return <MuiPlayCircleFilledIcon className="PlayCircleFilledIcon" />;
};

export default PlayCircleFilledIcon;
