import React, {FC} from 'react';
import MuiDescriptionIcon from '@material-ui/icons/Description';

import './DescriptionIcon.scss';

const DescriptionIcon: FC = () => {
  return <MuiDescriptionIcon className="DescriptionIcon" />;
};

export default DescriptionIcon;
